div(ng-show="tab==2")
  #proposal-list(ng-controller="ctrl.proposal")
    .proposal(ng-repeat="p in list")
      .ib
        .stick
          .label.label-success 已投
        .thumb
        .name {{p.name}}
        .desc {{p.desc}}
        .detail
          .label.label-danger 投票中
          .label.label-warning 投票率50%
